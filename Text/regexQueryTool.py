# A tool that allows the user to enter a text string and then in a separate control enter a regex
# pattern. It will run the regular expression against the source text and return any matches or flag errors in the
# regular expression.

import sys, os 

def main():
    # Code here

if __name__ == "__main__": 
    main()